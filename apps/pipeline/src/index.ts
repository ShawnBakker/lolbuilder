/**
 * M2 — data pipeline (fetch → deserialize → normalize → shrink → shard →
 * publish). Opens per plan (docs/plan/pick-analyzer-plan.md §M2); the M1
 * dry-run exercises a 2–3 champion slice first. Placeholder until then.
 */
export {};
