/**
 * M5 — Vite+React frontend (draft board + honest-uncertainty UI). Opens per
 * plan (docs/plan/pick-analyzer-plan.md §M5); the Vite scaffold lands when
 * M5 opens. M1's Pages bootstrap deploys a static placeholder page, not this
 * package. Placeholder until then.
 */
export {};
